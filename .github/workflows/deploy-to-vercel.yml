name: Deploy to Vercel

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Vercel CLI
        run: npm install -g vercel

      - name: Link to Vercel Project
        run: vercel link

      - name: Deploy Frontend to Vercel
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          cd /root/.openclaw/workspace/game-monitor/frontend
          npm install
          npm run build
          vercel --prod --yes

      - name: Deployment Summary
        if: always()
        run: |
          echo "✅ Frontend deployed successfully!"
          echo "📍 URL: https://game-monitor.vercel.app (hoặc URL từ Vercel output)"
          echo "📊 Worker: https://global-game-monitor-worker.duy-nk0112.workers.dev"
