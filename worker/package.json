{
  "name": "global-game-monitor-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Global Game Monitor RSS proxy",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc"
  },
  "dependencies": {
    "itty-router": "^4.0.20"
  },
  "devDependencies": {
    "wrangler": "^3.78.0",
    "@cloudflare/workers-types": "^4.20240529.0",
    "typescript": "^5.5.0"
  }
}
